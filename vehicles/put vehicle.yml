info:
  name: put vehicle
  type: http
  seq: 1

http:
  method: PUT
  url: http://127.0.0.1:5000/vehicle/121323
  body:
    type: json
    data: |-
      {
          "capacity": "175",
          "driver_id": 1,
          "model": "Toyota"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
